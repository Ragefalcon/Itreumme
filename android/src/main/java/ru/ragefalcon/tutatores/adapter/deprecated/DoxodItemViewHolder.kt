package ru.ragefalcon.tutatores.adapter.deprecated

/*

class DoxodItemViewHolder (bindingView: ItemRasxodBinding, val stateViewModel: MyStateViewModel):
    RVMainAdapterViewHolder<ItemCommonFinOper, ItemRasxodBinding>(bindingView){

    override fun adaptF(item: ItemCommonFinOper) {
        with(binding) {
            textName.text = item.name
            textType.text = item.type
            textSchet.text = item.schet
            textSumma.text = item.summa.toString()
            textData.text = Date(item.data).format("dd MMM yyyy")
//        text_name_oper.setOnClickListener {
////            print("aaa")
//        }
        }
        this.itemView.setOnClickListener {
            print("bbb")
        }
        this.itemView.setOnLongClickListener {
            Log.d("item", "До ${item.name}")
            stateViewModel.changeItemCommonFinOper = item
            stateViewModel.visChangeFinPanel.value = true
//            print("item: ${item.name}")
            true
        }
    }

    override fun onItemSelected() {
    }

    override fun onItemCleared() {
    }

}*/
